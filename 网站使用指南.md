# DiReC Lab 网站使用指南 | Website User Guide

## 目录 | Table of Contents

1. [启动网站](#启动网站)
2. [常见修改操作](#常见修改操作)
3. [文件结构说明](#文件结构说明)
4. [颜色和主题调整](#颜色和主题调整)
5. [添加新内容](#添加新内容)
6. [部署到线上](#部署到线上)

---

## 启动网站

### 启动本地开发服务器

```bash
# 1. 进入网站目录
cd /Users/yizhi/direc-lab

# 2. 启动服务器（显示草稿）
hugo server -D

# 3. 在浏览器打开
# http://localhost:1313/
```

**提示：** 当服务器运行时，你修改文件后浏览器会自动刷新显示最新内容。

### 停止服务器

按 `Ctrl + C` 停止服务器

---

## 常见修改操作

### 1. 修改首页内容

**文件位置：** `content/_index.md`

```bash
# 用编辑器打开
code content/_index.md
# 或者
nano content/_index.md
```

**可以修改的内容：**
- 标题和描述文字
- 研究方向描述
- 添加/删除板块

**示例 - 修改标题：**
```yaml
title: |
  DiReC Lab
  Digital Innovation and Research in Construction
```

### 2. 修改联系信息

**文件位置：** `content/contact/index.md`

```yaml
email: direc@tamu.edu        # 修改邮箱
phone: (979) 458-9380        # 添加电话
address:
  street: Francis Hall       # 修改地址
  city: College Station
  region: TX
```

### 3. 修改网站标题和基本信息

**文件位置：** `config/_default/hugo.yaml`

```yaml
title: DiReC Lab - Digital Innovation and Research in Construction
baseURL: 'https://your-domain.com/'  # 修改为你的域名
```

**文件位置：** `config/_default/params.yaml`

```yaml
marketing:
  seo:
    org_name: 'DiReC Lab'
    description: '修改你的实验室描述...'
```

---

## 文件结构说明

```
direc-lab/
├── config/               # 网站配置文件
│   └── _default/
│       ├── hugo.yaml    # 基本设置（标题、URL等）
│       ├── params.yaml  # 主题、SEO、功能设置
│       └── menus.yaml   # 导航菜单
│
├── content/             # 所有网站内容
│   ├── _index.md        # 首页
│   ├── authors/         # 团队成员
│   │   ├── admin/       # Dr. Jeong（主任）
│   │   ├── suryeon-kim/ # 成员示例
│   │   └── ...
│   ├── publication/     # 发表论文
│   ├── post/           # 新闻动态
│   ├── people/         # 团队页面
│   └── contact/        # 联系页面
│
├── data/
│   └── themes/
│       └── direc_bright.toml  # 自定义颜色主题
│
├── static/             # 静态文件（图片、PDF等）
└── assets/            # 资源文件
```

---

## 颜色和主题调整

### 修改颜色方案

**文件位置：** `data/themes/direc_bright.toml`

```toml
[light]
  # 主色调 - 天空蓝
  primary = "#0ea5e9"          # 改为你想要的颜色
  primary_light = "#38bdf8"    # 浅色变体
  primary_dark = "#0284c7"     # 深色变体

  # 背景颜色
  background = "#ffffff"       # 页面背景
  background_2 = "#f1f5f9"     # 次要背景

  # 文字颜色
  text = "#1f2937"             # 主文字
  text_light = "#6b7280"       # 次要文字
```

### 推荐的明亮色系参考

```toml
# 天空蓝（当前）
primary = "#0ea5e9"

# 青色系
primary = "#14b8a6"

# 翠绿色
primary = "#10b981"

# 琥珀色
primary = "#f59e0b"

# 玫瑰色
primary = "#f43f5e"

# 紫色
primary = "#8b5cf6"
```

### 使用在线工具选择颜色

1. **Tailwind Color Picker**: https://tailwindcss.com/docs/customizing-colors
2. **Color Hunt**: https://colorhunt.co/
3. **Coolors**: https://coolors.co/

**提示：** 修改颜色后，保存文件，浏览器会自动更新。

---

## 添加新内容

### 添加新的团队成员

#### 方法1：使用 Hugo 命令（推荐）

```bash
# 创建新成员（假设叫 John Doe）
hugo new content/authors/john-doe/_index.md
```

#### 方法2：手动创建

```bash
# 1. 创建目录
mkdir -p content/authors/john-doe

# 2. 创建个人资料文件
# 复制已有成员的 _index.md 作为模板
cp content/authors/suryeon-kim/_index.md content/authors/john-doe/_index.md

# 3. 编辑文件
code content/authors/john-doe/_index.md
```

**编辑内容：**

```yaml
---
title: John Doe
first_name: John
last_name: Doe
role: Ph.D. Student

organizations:
  - name: Texas A&M University
    url: 'https://www.tamu.edu'

bio: 简短的个人简介

interests:
  - 研究兴趣1
  - 研究兴趣2

education:
  courses:
    - course: M.S. in XXX
      institution: 大学名称
      year: 2020

social:
  - icon: envelope
    icon_pack: fas
    link: 'mailto:email@tamu.edu'

email: 'email@tamu.edu'

user_groups:
  - Ph.D. Students    # 或 Postdoctoral Researchers, Alumni 等
---

详细的个人介绍...
```

#### 添加个人照片

```bash
# 将照片命名为 avatar.jpg 或 avatar.png
# 放入成员目录中
cp /path/to/photo.jpg content/authors/john-doe/avatar.jpg
```

**照片要求：**
- 格式：JPG 或 PNG
- 建议尺寸：512x512 像素
- 文件名：必须是 `avatar.jpg` 或 `avatar.png`

---

### 添加新的发表论文

```bash
# 1. 创建论文目录（用论文简称或年份命名）
mkdir -p content/publication/2024-new-paper

# 2. 创建论文信息文件
code content/publication/2024-new-paper/index.md
```

**模板：**

```yaml
---
title: "论文完整标题"
authors:
- admin                    # Dr. Jeong
- suryeon-kim             # 其他作者
date: "2024-01-15T00:00:00Z"
doi: "10.1061/XXXXX"      # DOI号

publication_types: ["article-journal"]  # 或 "paper-conference"

publication: "*期刊名称*, 卷(期)"
publication_short: "期刊缩写"

abstract: |
  论文摘要...

summary: 一句话总结

tags:
- 标签1
- 标签2

featured: true            # 是否在首页展示

# 链接
url_pdf: 'https://...'
url_source: 'https://doi.org/...'
---

论文的详细描述...
```

---

### 添加新闻动态

```bash
# 1. 创建新闻目录
mkdir -p content/post/2024-new-award

# 2. 创建新闻文件
code content/post/2024-new-award/index.md
```

**模板：**

```yaml
---
title: 新闻标题
date: 2024-11-16
# draft: true           # 取消注释表示草稿（不发布）
---

新闻的简短摘要（会显示在列表页）

<!--more-->

新闻的详细内容...

- 可以使用列表
- **粗体文字**
- [链接](https://example.com)

![图片](image.jpg)
```

#### 添加新闻图片

```bash
# 将图片放在同一目录下
cp /path/to/image.jpg content/post/2024-new-award/featured.jpg
```

---

### 修改导航菜单

**文件位置：** `config/_default/menus.yaml`

```yaml
main:
  - name: Home           # 菜单显示名称
    url: /              # 链接地址
    weight: 10          # 排序（数字越小越靠前）

  - name: People
    url: people
    weight: 20

  - name: Publications
    url: publication
    weight: 30

  - name: News
    url: post
    weight: 40

  - name: Contact
    url: contact
    weight: 50

  # 添加新菜单项
  - name: Research      # 新菜单
    url: research
    weight: 25
```

---

## 实用操作示例

### 示例1：修改研究方向描述

**文件：** `content/_index.md`

找到这一段：

```markdown
<h3 style="color: #0ea5e9; margin-top: 0;">🤖 Project Intelligence</h3>
<p>Automating interpretation of project documents and generating solutions through AI and data analytics</p>
```

修改为你想要的描述：

```markdown
<h3 style="color: #0ea5e9; margin-top: 0;">🤖 项目智能</h3>
<p>你的新描述文字...</p>
```

### 示例2：添加实验室 Logo

```bash
# 1. 准备 logo 文件（建议 PNG 格式，透明背景）
cp /path/to/logo.png assets/media/logo.png

# 2. 修改配置文件
# 编辑 config/_default/params.yaml
```

在 `header:` 部分添加：

```yaml
header:
  navbar:
    enable: true
    align: r
    show_logo: true      # 确保这个是 true
    logo: "logo.png"     # logo 文件名
```

### 示例3：修改页脚版权信息

**文件：** `config/_default/params.yaml`

```yaml
footer:
  copyright:
    notice: '© {year} DiReC Lab, Texas A&M University. All rights reserved.'
```

修改为：

```yaml
footer:
  copyright:
    notice: '© {year} 你的实验室名称. 保留所有权利。'
```

---

## 构建和部署

### 本地构建（生成静态文件）

```bash
# 构建网站
hugo

# 生成的文件在 public/ 目录中
ls public/
```

### 部署到 GitHub Pages

```bash
# 1. 创建 GitHub 仓库
# 在 GitHub 创建一个新仓库，例如：direc-lab-website

# 2. 初始化 Git（如果还没有）
git init
git add .
git commit -m "Initial commit"

# 3. 关联远程仓库
git remote add origin https://github.com/你的用户名/direc-lab-website.git
git branch -M main
git push -u origin main

# 4. 在 GitHub 仓库设置中启用 GitHub Pages
# Settings -> Pages -> Source: GitHub Actions
```

### 部署到 Netlify（推荐）

1. **注册 Netlify 账号**: https://www.netlify.com/
2. **连接 GitHub 仓库**
3. **配置构建设置**:
   - Build command: `hugo`
   - Publish directory: `public`
4. **点击 Deploy**

**已包含 Netlify 配置文件：** `netlify.toml`

---

## 常见问题

### Q1: 修改后网站没有变化？

**答：**
1. 确认 Hugo 服务器正在运行
2. 检查浏览器是否自动刷新
3. 手动刷新浏览器（Cmd+R 或 Ctrl+R）
4. 清除浏览器缓存（Cmd+Shift+R 或 Ctrl+Shift+R）

### Q2: 添加的团队成员没有显示？

**答：** 检查以下几点：
1. 文件是否保存在正确位置：`content/authors/[姓名]/_index.md`
2. `user_groups:` 是否正确设置（必须与 `content/people/index.md` 中的组名匹配）
3. 文件格式是否正确（YAML 前置内容格式）

### Q3: 颜色修改后没有生效？

**答：**
1. 确认修改的是 `data/themes/direc_bright.toml`
2. 确认 `config/_default/params.yaml` 中的主题设置为 `direc_bright`
3. 重启 Hugo 服务器：`Ctrl+C` 停止，然后重新运行 `hugo server -D`

### Q4: 如何备份网站？

**答：**
```bash
# 方法1：使用 Git（推荐）
git add .
git commit -m "网站更新"
git push

# 方法2：压缩整个目录
cd /Users/yizhi/
tar -czf direc-lab-backup-$(date +%Y%m%d).tar.gz direc-lab/
```

### Q5: 如何恢复到之前的版本？

**答：**
```bash
# 如果使用 Git
git log                    # 查看提交历史
git checkout [commit-id]   # 恢复到指定版本
```

---

## 进阶技巧

### 1. 批量添加论文

如果你有很多论文要添加，可以编写脚本：

```bash
#!/bin/bash
# add_publications.sh

while IFS=',' read -r title authors year journal doi; do
    # 创建目录
    slug=$(echo "$title" | tr '[:upper:]' '[:lower:]' | tr ' ' '-' | cut -c1-30)
    mkdir -p "content/publication/$year-$slug"

    # 创建文件
    cat > "content/publication/$year-$slug/index.md" <<EOF
---
title: "$title"
authors: [$authors]
date: "$year-01-01T00:00:00Z"
doi: "$doi"
publication_types: ["article-journal"]
publication: "$journal"
---
EOF
done < publications.csv
```

### 2. 自动化部署

创建 GitHub Actions 工作流：

**文件：** `.github/workflows/hugo.yml`

```yaml
name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'
      - name: Build
        run: hugo --minify
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
```

---

## 获取帮助

- **Hugo 官方文档**: https://gohugo.io/documentation/
- **Hugo Blox 文档**: https://docs.hugoblox.com/
- **Hugo 中文社区**: https://hugo.aiaide.com/

---

## 快速命令参考

```bash
# 启动开发服务器
hugo server -D

# 构建网站
hugo

# 创建新内容
hugo new content/authors/name/_index.md
hugo new content/post/2024-news/index.md
hugo new content/publication/2024-paper/index.md

# 检查 Hugo 版本
hugo version

# 更新模块
hugo mod get -u

# 清理缓存
hugo mod clean
```

---

**祝你使用愉快！如有问题，随时查阅本指南。** 📚
